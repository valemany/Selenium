# set the correct runtime
runtime: ruby21

#Run cucumber selenium tests with chrome
test: |
  if [[ ! -n "${ROCK_ARG1}" ]]; then
    die "Usage: rock test SITE [TAG]"
  fi


  LANGUAGE="${ROCK_ARG2}" SITE="${ROCK_ARG1}" exec rspec --tag "${ROCK_ARG2}"